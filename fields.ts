
//not done btw
const wrapper = document.getElementById('fields-wrapper') as HTMLDivElement | null;
const addButton = document.getElementById('add-field-btn') as HTMLElement | null;

if (wrapper && addButton) {
    addButton.addEventListener('click', (e:MouseEvent): void => {

    })
}